# 项目进度文件

TLDR: 本文件为当前项目唯一进度文件，按“先最小闭环，后全量扩展”推进；每轮对话结束后更新“本轮完成 / 当前未完成 / 下一步”。

## 一、项目目标
- 将多源资料整理为可入 Dify 的带元数据标签 Markdown。
- 以结构化 JSON + 模板化 Markdown 形成最小可执行闭环。
- 在样例跑稳后，再接入真实解析链路、批量处理和入库流程。

## 二、当前执行口径
- 当前阶段：最小闭环已落地，继续补真实解析与批量能力。
- 推进顺序：先最小闭环，后全量扩展。
- 唯一进度文件：`知识库结构化入Dify准备/01_执行计划.md`。
- 记录规则：每轮对话后追加更新“本轮完成 / 当前未完成 / 下一步”。
- 敏感边界：配置文件中的 API Key、Token、Webhook、口令不得写入本文件正文。

## 三、当前已完成
### 1. 最小闭环
- 已建立最小工作流主链，产出 `structured.json`、`structured.md`、`status.json`、`extracted.txt`。
- 已建立保守分流与处理路径：`text_direct`、`document_parse`、`ocr`、`manual_review`。
- 已实现 OCR 占位策略，扫描型 PDF/图片可保守落为待 OCR。
- 输出契约版本已推进到 `0.5.0`。

### 2. 模板能力
- 已跑通政策官方文件模板。
- 已跑通供应商企业模板。
- 已跑通单位联系人模板。
- 已跑通方案案例模板。
- 已跑通产品设备模板。

### 3. 样例与回归
- 已纳入首轮样例注册表：
  - `nw_report_fy_q4`
  - `nw_supplier_huanuoxingkong`
  - `policy_uav_regulation`
  - `scenario_heilongjiang_2024`
  - `solution_zhongshan_medical`
  - `supplier_yuefei_2025`
  - `policy_chongqing_txt_review`
- 已完成 12 个测试的稳定回归。
- 已验证 OCR 占位时 `处理路径` 正确落为 `ocr`。
- 已修正供应商、方案、产品三类样例的主要字段抽取问题。
- 已为产品设备模板补入质检/检验/检定/校准报告字段映射，可回填报告文档类型、产品编号、检定依据、检定结果、检测机构、报告编号、报告日期、有效期至。

### 4. 已核实的外部参考资产
- 已核实 `配置文件.txt` 存在 Qwen 兼容模式地址、Qwen 模型名、MinerU 文档地址、MinerU 凭证、旧质检抽取项目路径。
- 已核实旧质检报告抽取项目路径：`D:/changfeng/临时dock配置/检验报告数据抽取`。
- 已核实旧项目中可复用资产：
  - `脚本参考.txt`：文件读取、Prompt 抽取、Markdown 生成、Dify 上传原型。
  - `report_extract/extract_prompt.txt`：质检报告结构化抽取提示词。
  - `report_extract/mineru_converter.py`：MinerU 批量上传、轮询、下载、清洗、拆分流程参考。

## 四、当前未完成
### 1. 真实解析链路
- 未接入真实 OCR。
- 未接入真实 MinerU 主处理链。
- 未接入真实 Qwen-Plus 分类与字段抽取。

### 2. 样例覆盖缺口
- `nw_report_fy_q4` 仍为 OCR placeholder。
- `policy_uav_regulation` 仍为 OCR placeholder。
- `nw_supplier_huanuoxingkong` 的真实大画册规则仍需增强。
- 单位联系人模板仍缺真实样例回归。

### 3. 工作流与入库
- 未接入 Dify 正式上传流程。
- 未形成批量处理清单与分批入库节奏。
- 未完成真实解析链下的去重、证据等级、拆层复核。

## 五、本轮完成
- 已再次核实当前目标目录的真实文件格式分布，并确认图片素材占比很高：
  - `D:/新维度测试资料`：共 268 个文件，`jpg 74`、`md 65`、`pdf 52`、`docx 42`。
  - `D:/低空经济相关资料汇总（李伟）整理2025-05-23至2026-03-31 - 副本`：共 453 个文件，`pdf 225`、`docx 66`、`png 45`、`jpg 35`、`jpeg 29`。
  - `D:/长风资料 - 副本`：共 6628 个文件，`jpg 5462`、`md 757`、`json 161`、`pdf 95`。
- 已确认“纯照片直接跳过，不做转换”应作为正式执行规则。
- 已把最小工作流解析层补到更贴近真实目录格式：新增 `json`、`csv`、`log`、`ppt`、`pptx`、`webp` 的识别与处理路径。
- 已在提取层增加图片分流规则：
  - 命中文档关键词的图片进入 OCR 占位层。
  - 纯照片、现场图、活动图、航拍图等图片直接 `skip`。
- 已把 `skip` 路径接入 pipeline 分流判断，纯照片可稳定落为 `处理路径=skip`、`分流结果=跳过`。
- 已更新输出契约到 `0.5.1`，补入 `skip` 处理路径说明，并扩展文本直读与文档解析口径。
- 已新增 3 条解析层相关测试，当前最小工作流测试共 18 条，已全部通过。
- 已把目录格式核实结果、解析分流矩阵、纯照片跳过规则写入 `技术落地细则.md`。
- 已把标准执行顺序、格式识别与分流口径、图片补充规则写入 `自动化执行手册.md`。
- 已重写 `02_分类建议.md`，补齐一级分类体系、模板映射、自动化分类口径、输出字段与准确率评估建议。
- 已把 MinerU 作为可选 PDF 抽取策略接入 CLI：支持 `--pdf-extractor local|mineru` 与 `--mineru-token` / `MINERU_TOKEN`。
- 已在 pipeline 增加按策略切换 PDF 抽取入口，可在本地解析与 MinerU 间切换。
- 已补 MinerU CLI 接入相关单元测试，当前最小工作流测试共 23 条，已全部通过。
- 已完成 `policy_uav_regulation` 的 MinerU 真实验证：成功返回 Markdown，文本长度约 12480，批次号 `232d30d4-6255-4284-8d96-bfac660729ee`。
- 已修正政策字段抽取规则：补入政策标题、发文字号、发文单位、成文日期归一化、生效状态判断与章节型核心任务抽取。
- 已补 1 条政策 MinerU 字段回归测试，当前最小工作流测试共 24 条，已全部通过。
- 已接入真实 Qwen 分类与字段补强链路：
  - `runtime_config.py` 已接入运行时配置读取。
  - `qwen_client.py` 已接入 OpenAI 兼容调用、JSON 解析、字段白名单与列表字段规范化。
  - `pipeline.py` 已接入 Qwen 分类合并逻辑，分类字段允许更新，规则已产出的业务字段优先保留。
  - `cli.py` 已增加 `--enable-qwen`、`--qwen-api-key`、`--qwen-base-url`、`--qwen-model`，并支持从 `配置文件.txt` 回退读取 MinerU / Qwen 配置。
- 已补 Qwen 接入与展示层回归测试，当前最小工作流测试共 34 条，已全部通过。
- 已按“先抽样再扩大范围”的口径完成 3 个真实样例核实：
  - `policy_uav_regulation`：稳定落为 `政策法规与官方通知库 / 国家级行政法规 / 政策官方文件模板`。
  - `supplier_yuefei_2025`：稳定落为 `供应商企业与厂家资料库 / 企业画册与产品目录 / 供应商企业模板`。
  - `solution_zhongshan_medical`：稳定落为 `方案案例与实施资料库 / 低空医疗转运方案 / 方案案例模板`。
- 已按用户确认口径保留跨模板字段：
  - 跨模板字段继续写入 `structured.json`。
  - Markdown 元数据区展示一级/二级分类、分类置信度、分类依据。
  - Markdown 正文新增“模型补充字段”区，用于展示 Qwen 补出的可复核字段。
- 已完成单位联系人模板首个真实样例回归：
  - 新增样例 `contact_chongqing_public_data`，源文件为 `D:/aurora/Aurora知识库/Aurora知识库/Raw sources/Inbox/重庆申请公开数据资料.txt`。
  - 已按“联系人不足时只保留单位级和接口级线索”的规则完成样例接入与真实输出验证。
- 已增强单位联系人规则抽取：
  - 新增 `对接线索字段`，可提取接口地址、支持格式、请求方式、接口用途。
  - 已明确过滤 `localhost`、秘钥参数与部署命令，不把技术实现细节写入联系人正文。
  - 单位联系人 Markdown 展示层已补入“对接线索”展示，并将单位摘要改为优先展示单位级信息与接口级线索。
- 已补 2 条单位联系人相关回归测试，当前最小工作流测试共 37 条，已全部通过。
- 已完成 `contact_chongqing_public_data` 的真实样例输出验证，当前稳定落为“单位联系人模板 / 待审核 / 单位级信息+接口级线索保留”。
- 已增强 `nw_supplier_huanuoxingkong` 大画册规则：
  - 已补供应商画册的产品线兜底抽取，可稳定识别 `全域安防 / 安全应急 / 智能感知 / 无人系统 / 智慧交通`。
  - 已补华诺星空类大画册的关键产品兜底抽取，可提取低空防御体系相关设备与系统名称。
  - 已补供应商能力标签兜底，新增“智能感知与低空防御”能力归纳。
  - 已清洗大画册脏值，避免泛词条和乱码产品名进入 `核心产品`。
- 已完成 `nw_supplier_huanuoxingkong` 真实样例复跑验证，当前可稳定提取企业名称、主营方向、关键产品与核心能力。
- 已补 1 条华诺星空大画册回归测试，当前最小工作流测试共 38 条，已全部通过。
- 已继续接入 2 份同类厂家大画册抽样样例：
  - `supplier_united_aircraft_brochure`
  - `supplier_zhongsheng_aiot_brochure`
- 已完成两份样例的真实复跑核实：
  - `supplier_united_aircraft_brochure` 已可稳定识别集团主体“联合飞机集团”，并提取无人机产品族与主营方向。
  - `supplier_zhongsheng_aiot_brochure` 当前仍为扫描型宣传册，本地解析链落为 `ocr` 占位，文本长度为 `0`，需后续接真实 OCR。
- 已增强供应商大画册主体识别与无人机厂家规则：
  - 企业名称优先取正文中的集团/股份公司主体，避免误落首页子公司名称。
  - 已补联合飞机类画册的无人机型号兜底抽取与主营方向归纳。
- 已补 1 条联合飞机集团大画册回归测试，当前最小工作流测试共 39 条，已全部通过。
- 已接入最小真实 OCR fallback：
  - `minimum_workflow` 新增 `--enable-ocr` 开关。
  - 文档型图片与扫描型 PDF 可在保留现有契约的前提下尝试真实 OCR。
  - OCR 失败时继续保守回退到现有 `ocr:placeholder` 结果，不阻断结构化产物生成。
- 已补真实 OCR 相关回归测试：覆盖扫描型 PDF 成功/失败回退、文档型图片成功/失败回退、OCR 路由兼容，当前最小工作流测试共 45 条，已全部通过。
- 已完成 `supplier_zhongsheng_aiot_brochure` 的真实 OCR 样例回归：
  - `OCR-only` 下已从 `ocr:placeholder:pdf` 提升为 `ocr:mineru:pdf`。
  - 当前文本长度约 `18431`，页数 `25`，`structured.md` 已不再是“当前尚未提取到可用正文”。
- 已完成 `supplier_zhongsheng_aiot_brochure` 的 `OCR + Qwen` 联合核实：
  - 已补出主营方向“智能视频AIOT平台与AI+场景解决方案”。
  - 已补出一级/二级分类与多项补充字段，说明扫描型宣传册已能进入后续结构化链路。

## 六、当前未完成
1. MinerU 已完成本地接口封装、真实初测、Markdown 清洗、CLI 可选接入与最小 OCR fallback 接入，但尚未正式切到当前默认主抽取分支。
2. 单位联系人模板已补首个真实样例回归，但仍需继续补更多真实样例，验证公开联系人、单位级线索、脱敏边界三类情况。
3. 华诺星空与联合飞机两类厂家大画册已完成抽样复核；中盛益华扫描型宣传册也已打通 OCR，但软件平台型宣传册的主体识别、主营方向和产品归纳规则仍需继续增强。
4. 未接入 Dify 正式上传流程。
5. 商务报价与招采合同、内部方法流程与话术两类资料仍未形成正式模板与稳定字段抽取规则。
6. Qwen 已接通并完成首轮样例核实，但进入更大范围执行前仍需继续按批次抽样复核。
7. 政策类已明确改为“解析层 + 规则脚本优先”，后续需在更多政策样例中继续验证该口径稳定性。

## 七、下一步
1. 继续补单位联系人模板更多真实样例，重点覆盖“有公开联系人”“仅单位级线索”“需脱敏”三类边界。
2. 继续增强软件平台型供应商宣传册规则，优先修正中盛益华这类样例的主体识别、主营方向与产品归纳。
3. 接着决定哪些 PDF / Office 资料默认优先走 MinerU，哪些继续保守走本地解析。
4. 政策类样例默认继续走“解析层 + 规则脚本直出”，不再默认调用 Qwen，后续只做规则增强与样例验证。
5. 再推进 Dify 正式上传流程接入。
